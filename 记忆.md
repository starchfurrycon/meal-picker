# 选餐 · 项目记忆

> 结档于 **v2.1.1**。这份文件记的是"下次回来还用得上"的东西：
> 怎么跑、东西在哪、以及那几个**花了很久才查出来、忘掉就会重新踩一遍**的坑。
>
> 详细使用说明看 `README.md`；这份只记关键结论和来龙去脉。

---

## 一、这是什么

一个多平台外卖**实时比价**小工具（中文界面）。用户说一句"想吃点辣的，一个人，四十以内"，
它在 美团 / 饿了么 / 淘宝闪购 / 京东 四个平台上按加权公式挑出**性价比最高的那一个套餐**，
用小程序分享卡的样子呈现出来。

两条硬性原则，是用户明确要求、不能违背的：

1. **不用模拟数据。** 拿不到实时价就直说，绝不用假价格凑数。
2. **不复用缓存价。** 每轮比价开始时中继会整体清空价格表（`POST /api/collect` 里
   `priceStore = new Map()`），保证每次看到的都是当下抓的。

## 二、在哪

| 项目 | 地址 |
|---|---|
| 仓库 | https://github.com/starchfurrycon/meal-picker （public，`main`） |
| 在线版 | https://starchfurrycon.github.io/meal-picker/ |
| 发行版 | https://github.com/starchfurrycon/meal-picker/releases |

当前版本 **2.1.1**。已发布的 tag：`v1.0.0` `v2.0.1` `v2.1.0` `v2.1.1`
（`v2.0.0` 是个坏掉的发行，已从本地和远端一并删除，别再打这个号）。

## 三、怎么跑起来

**给用户的两步**（发行包 `meal-picker-2.1.1.zip` 解开后）：

```bash
node relay/server.mjs          # 或双击 relay/启动.cmd
```

然后浏览器开 `http://127.0.0.1:8765/`。中继会自己拉起一个浏览器实例并注入采集器，
**用户只需要在那个窗口里把几个平台登录一次**，之后每次都是实时价。

**开发时**：

```bash
npm start          # 起中继
npm run build      # 生成采集器 + 打包到 dist/（单文件 + 目录版 + zip + 校验和）
npm run collector  # 只重新生成采集器（改了 collector-core.js 之后必须跑）
npm test           # 42 项
npm run e2e        # 23 项，真实浏览器 + 假采集器灌数据
npm run e2e:llm    # 9 项，本机假 LLM 接口，不花钱
npm run e2e:dist   # 10 项，发行包自检
npm run e2e:browser# 10 项，真开浏览器验注入与回传通道
```

全部自检 **94 项**，不花钱、不需要真 Key。`e2e:browser` 会打开真实平台搜索页，
但**只检查采集器有没有被注入，不读数据**。

## 四、架构

```
中继自己拉起的浏览器（独立配置目录 data/browser-profile，不碰你日常那个）
  ├─ 平台页 https://waimai.meituan.com/...  ← 采集器被 CDP 注入到 document_start
  │     采集器只读页面自己请求回来的数据，不抓取、不代替登录、不绕风控
  │     数据经 CDP binding 回到中继（不经过网络，见坑 #3）
  └─ http://127.0.0.1:8765/  ← 工具页面（也可以用自己的浏览器开）
        中继 relay/server.mjs：管任务、收价格、清缓存
```

**为什么工具页面要由中继托管？** 因为 GitHub Pages 是 https，而中继是
`http://127.0.0.1`。https 页面碰不到回环地址（见坑 #3），所以 Pages 上的页面
连不上中继。`http://127.0.0.1:8765/` 是同源 http，没这个问题。
`file://` 打开单文件版也能连中继（CORS 是 `*`）。

**其它要点**：

- 技术栈是**零构建单页 HTML + 原生 ESM**，双击就能开，也能 gh-pages 部署。
- 保险箱：WebCrypto AES-GCM 256 + PBKDF2-SHA256（310k 次），默认设备密钥自动解锁，
  可选口令。账号信息不会明文落盘。
- 加权公式 8 个因子：价格 26 / 优惠 18 / 券 12 / 评分 14 / 商家信誉 10 /
  匹配度 10 / 时效 6 / 会员 4。
- LLM 是**可选**的，用户自己填 OpenAI 兼容的 Base URL + Key，浏览器直连
  `/v1/chat/completions`。它**只把模糊描述改写成搜索关键词**，不给最终答案——
  所以提示词刻意做得极省（短 system、`max_tokens: 180`、`temperature: 0.2`、
  强制 JSON），并且会把 token 和估算花费显示出来。不开 LLM 时用内置的轻量语义提取。

## 五、踩过的坑（最重要的一节）

### 坑 1：`tokens.css` 漏引，整个目录版崩坏

`web/index.html` 只引了 `app.css`，漏了 `styles/tokens.css`。所有设计令牌因此是空的，
`body` 保留浏览器默认的 8px 外边距、`overflow: visible`、透明背景，而左下角"设置"的
`bottom: max(var(--s-5), env(...))` 直接解析成 `bottom: auto`，跑到页面中间**压在标题上**。

阴险的地方：**这个 bug 从 1.0.0 就在，而且只影响目录版/Pages 版**。单文件版是把两份
CSS 内联进去的，一直正常，所以五套自检全都没抓到。

现在有三道防线，别再拆掉：

- 目录版打包也内联 CSS，并且**发现还有外链 `rel="stylesheet"` 就直接报错**；
- `e2e.mjs` / `e2e-dist.mjs` 各有"设计令牌真的生效 + 设置入口贴底不压标题"的几何断言。

### 坑 2：Chrome 153 禁掉了 `--load-extension`

本来想让中继用 `--load-extension` 拉起一个"已装好采集器扩展"的浏览器，
**Chrome 153 上这条路被禁死了**：`--load-extension`、`--enable-unsafe-extension-debugging`、
`--disable-features=DisableLoadExtensionCommandLineSwitch` 三个开关全部无效。
（Edge 153 仍然可用，但只有 Edge 能用不够。）

所以改走 **CDP 注入**，这在所有 Chromium 系浏览器上都成立。复现：

```bash
node scripts/probe-extension.mjs   # 命令行加载扩展：Chrome ✘ / Edge ✔
node scripts/probe-launch.mjs      # CDP 注入：Chrome ✔
```

### 坑 3：Private Network Access —— 零安装的命门

平台页全是 https，中继是 `http://127.0.0.1`。直觉会以为是"混合内容"问题，
**其实不是**——`127.0.0.1` 在 Chrome 眼里算可信来源，混合内容管不着它。

真正拦路的是 **Private Network Access**。在真实美团页面上实测：

```
Access to fetch at 'http://127.0.0.1:61754/api/health' from origin
'https://waimai.meituan.com' has been blocked by CORS policy:
Permission was denied for this request to access the `loopback` address
```

**采集器读得到价格，但一条都发不回来。** 而界面上的表现只是"没找到价格"，
完全看不出是通道坏了——这是最坑的地方。

解法是走 **CDP 的 `Runtime.addBinding`**：中继往页面注入一个 `window.__mealPickerRelay()`，
采集器调用它，数据经 DevTools 通道回到中继，**完全不经过网络栈**，
所以 CORS / PNA / 混合内容 / 页面 CSP 一个都管不着。

搜索任务同理：页面回头来拉也拉不动，所以中继在注入脚本时就把这一轮的关键词一起写进去
（`buildInjectSource`）。油猴脚本那条路不受影响，走 `GM_xmlhttpRequest`（扩展特权请求）。

复现：

```bash
npm run probe:channel   # 真实平台页上验证：fetch 确实被 PNA 拦
npm run probe:binding   # 验证 binding 在 document_start 就可用
npm run probe:live      # 端到端：注入 → 采集 → 回传 → 进价格表
```

`e2e-browser.mjs` 里有两条断言专门守这个（回传通道挂上了、任务写进注入脚本了）。
**别删**——这两点任何一个坏掉，表现都只是"没找到价格"。

### 坑 4：`Target.createTarget` 的注入时序

`Target.createTarget` 会**先触发 `targetCreated` 事件，那一刻注入就发生了**。
所以任务必须在 `createTarget` 之前设好（`open()` 里第一件事就是 `activeTask = task`），
否则先挂上的那份脚本里没有任务，采集器会一直等一个永远不来的活。

另外 `injectInto` 不能按 target 去重，得按"任务指纹"去重，否则补挂的那份会被跳过。

### 坑 5：固定端口上的僵尸中继会静默骗你

调试时被这个坑掉很久：上一次探测超时被强杀，**旧中继进程还占着端口**，
后面所有请求都打到那个旧进程上，看到的永远是旧代码的行为。
`probe-collector-live.mjs` 现在会先探端口，占用就直接报错退出。

排查命令：

```powershell
Get-CimInstance Win32_Process -Filter "Name='node.exe'" |
  Where-Object { $_.CommandLine -match 'server\.mjs' } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```

### 坑 6：CI 上的一串环境问题

- **e2e 必须固定用油猴模式**（`collectorMode: 'manual'`）。自动模式会真去拉浏览器、
  打开真实平台页，CI 上又慢又不稳。自动模式那条路由 `e2e-browser.mjs` 单独验。
- **Linux 没有图形会话时**（`DISPLAY` 未设置）托管浏览器自动退到无头。
- **容器/CI 里常以 root 运行**，Chrome 沙箱会直接拒绝启动，要加 `--no-sandbox`
  `--disable-dev-shm-usage`。
- **`taskkill` 只在 Windows 上调**。Linux 上不存在这个命令，`spawn` 抛 ENOENT
  会把自检判成失败。
- **无头浏览器起不来时换端口重试**，并在失败时收掉孤儿进程。
- GitHub Actions 上 Node 20 已弃用，workflow 里统一 `node-version: '22'`
  （浏览器自检要用全局 `WebSocket` 连 CDP）。

### 坑 7：这台机器上的两个操作习惯

- **绝不要用 PowerShell 做文本替换来改项目文件**——中文会变乱码。
  用 `[System.IO.File]::WriteAllText($p, $t, (New-Object System.Text.UTF8Encoding($false)))`。
  （PowerShell 控制台里显示中文是乱码，但文件本身是对的 UTF-8，别被吓到。）
- **`git push` / `git tag` 即使成功也会报 `[exit code: 1]`**，因为 git 把进度写到
  stderr。看输出文字判断，别看退出码。

## 六、改代码时最容易忘的事

**`collector/collector-core.js` 是采集器的唯一真源。** 改了它之后必须跑
`npm run collector`（或 `npm run build`），它会生成两个产物：

- `collector/collector-inject.js` —— CDP 注入版
- `collector/meal-picker-collector.user.js` —— 油猴脚本版

`relay/platform-urls.js` 也是唯一真源，`gen-collector.mjs` 会把它复制成
`web/js/platform-urls.js`（因为 Pages 只发布 `web/`，跨目录 import 会 404）。

**版本号散在 6 个地方，改版时必须一起动**：

1. `package.json` → `version`
2. `collector/collector-core.js` → `const VERSION`
3. `relay/server.mjs` → `/api/health` 里的 `version`
4. `web/js/app.js` → `globalThis.__mealPicker.version`
5. `collector/userscript-header.txt` → `@version`
6. `.github/workflows/release.yml` → `workflow_dispatch` 的 `default` tag

（`web/js/catalog.js` 里的 `version: 2` 是**设置结构版本**，不是产品版本，别跟着改。）

**发版流程**：改完 → 六个版本号对齐 → 跑全套 94 项 → `git push` →
打 tag `vX.Y.Z` 推上去 → Release workflow 会自动打包并发布。
工作流里会校验 `package.json` 和 tag 是否一致，不一致直接失败。

## 七、诚实的限制（别在界面上说满）

- **价格来自平台列表页**，也就是起送价 + 配送/打包费 + 可见优惠。
  **最终成交价要等结算页**——界面上有明说"以各平台结算页为准"，不要拿估算价冒充实付价。
- **四个平台都要先登录一次**。登录用的是滑块/短信/设备指纹那一套，
  风控会拒掉非人类请求，**光填账号密码拿不到实时价**——这是死路，别再试。
  唯一可行的路就是在用户自己的浏览器里、用他自己的登录态去采集。
- **美团会把桌面版 Chrome 直接跳到 App 下载页**（`/mobile/download/default`），
  登录后才会进搜索页。
- **平台的真实接口全被 WAF 挡着**，实测结果记在这儿省得重试：
  美团 `weapp/v1/poi/food` → HTTP 418 `riskLevel:199`；
  饿了么 `h5.ele.me/restapi/...` → `NO_URL_MATCHED`；
  京东 `api.m.jd.com/client.action?functionId=search` → `appid is not present`；
  淘宝闪购 `h5api.m.taobao.com/...h5search` → `RGV587_ERROR::SM`。
  官方 API 是商家/ISV 门槛，个人拿不到。
- **采集器的 `ADAPTERS` 是尽力而为的启发式**。`GET /api/debug` 会留最近 12 条原始样本，
  是给新平台调适配用的。
- 托管浏览器的配置目录**和你日常用的浏览器是分开的**，好处是不碰你的数据，
  代价是得在里面单独登录一次。

## 八、清理记录

结档时清掉了（都可重新生成）：

| 内容 | 大小 | 说明 |
|---|---|---|
| `data/browser-profile*` | 208 MB | 托管浏览器配置，**含平台登录态**，已删 |
| `.build-tmp/` | 2.5 MB | 各版本打包中间产物 |
| `dist/` | 0.8 MB | 打包产物，`npm run build` 可重建 |
| `docs/` | 空 | 空目录 |
| 系统临时目录探测残留 | 144 MB | `%TEMP%\mp-*` / `mealpicker-*` |

工作区从 **213 MB 降到 1.7 MB**（69 个跟踪文件）。
源码、`.git` 历史、README 全部保留，远端 `main` 与本地一致。

> 注意：删掉 `data/` 意味着**下次用的时候要在托管浏览器里重新登录一次平台**。
